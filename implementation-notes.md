# implementation-notes

仕様書のない自由課題なので、ここには「依頼文に無く、こちらで決めたこと」だけを残す。

## データ源の選定

依頼は「5時間制限と週間制限の使用量の可視化」。最初に既存ツール（`ccusage` v20 / `claude-monitor` v4）
を確認したが、どちらもローカル JSONL のトークン数から 5時間ブロックを推定する設計で、
週間制限は扱っていない。トークン量はクォータ消費と比例しないため、これらの再利用は見送った。

採用したのは statusLine の stdin JSON に来る `rate_limits`。公式ドキュメントに記載があり、
`/usage` と同じサーバー実測値。OAuth トークンを掘ってエンドポイントを直叩きする案も考えたが、
非公開 API に依存するので採らなかった。

## 決めたこと

- **収集は statusLine に相乗り**。常駐デーモンを増やさず、追加の認証もない。代償として
  「Claude Code のセッションが動いている間しかサンプリングできない」制約が付く。これは
  可視化の目的（作業中に残量を見る）とは合致するので許容した。
- **サンプルは変化時のみ追記**。statusLine は 1 ターンに複数回走るので、毎回書くと肥大する。
  前回行と比較して同値ならスキップし、4MB を超えたら後半だけ残す。
- **リセット済みの窓は「未取得」ではなく 0%**。Claude Code はリセット後に当該キーを落とすため、
  素直に実装すると「データなし」になる。`WindowState.rolledOver` を設けて 0% と明示した。
  次の窓の `resets_at` は最初の送信まで確定しないので、リセット時刻は出さない。
- **バーンレートは二段構え**。サーバーが返す % がほぼ整数刻みなので、直近 45 分に変化が
  1 点も無いことがある。その場合は窓全体の平均に落とす。負の傾き（リセット跨ぎ）は捨てる。
- **429 履歴はインクリメンタル走査**。`~/.claude/projects` は 879MB あり毎回の全走査は無理。
  JSONL が追記専用であることを利用してファイルごとのオフセットを覚える。初回のみフルスキャン、
  2 回目以降は 33ms。マーカー `"rateLimitType"` をバイト列で探し、ヒットした行だけ JSON パースする。
- **配置は `~/Applications`**。`LSUIElement` のメニューバー常駐アプリなので Dock には出さない。
- **自動起動は `SMAppService.mainApp`**。`~/Library/LaunchAgents` に plist を置く手もあるが、
  それだと「システム設定 → ログイン項目」の一覧と実体が二重管理になる。ヘルパーを持たない
  単体アプリなので `mainApp` で足りる。アドホック署名（`codesign -s -`）のままでも
  `register()` は通ることを実機で確認した。CLI (`--login-item`) と UI トグルは同じ関数を呼ぶ。

## ビルド環境でのハマり

`swiftly` 経由の Swift 6.1.3（`~/Library/Developer/Toolchains`）で `swift build` すると
SDK モジュールの再構築が走り、10 分経っても終わらなかった。`xcrun -f swiftc` で Xcode 同梱の
6.3.3 を使うと 6 秒。`scripts/build-app.sh` は `xcrun` 経由で解決している。

## 確認済みの範囲

- 収集フック: statusLine の表示を壊さないこと、同値をスキップすること、変化時に追記することを確認
- 実データ: 5時間 23% / 週間 35% / リセット 14:40・9/20 8:00 を取得
- 429 履歴: 22 件検出、直近 9/14 15:54。JSONL を直接 grep した結果と一致
- バーンレート: 合成データで 1%/5分 → 12.0 %/h、24%/30分 → 48.0 %/h を確認
- 枯渇予測: 84% / 48%/h → 20 分後の 11:06。窓リセットが先なら「到達せず」
- 窓リセット: `resets_at` を過ぎたサンプルで 0% 表示に落ちること

- ログイン項目: `--login-item` で register → status → unregister → status を往復し、
  `SMAppService` の状態が追随することを確認。`bogus` 等の不正な引数は exit 2。

**未確認**: ログイン時に実際に起動するか（ログアウトを挟まないと確かめられないため、
`SMAppService.mainApp.status == .enabled` を確認できたことで代用している）。

メニューバーの見た目は、こちらからは画面収録の TCC が取れず（Terminal.app も使い捨ての
キャプチャ用 `.app` も `SCStreamErrorDomain -3801`）、2026-09-15 にユーザーの目視で確認した。
